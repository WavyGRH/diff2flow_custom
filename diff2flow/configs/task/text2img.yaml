# Text-to-Image task config
task_type: "text2img"
conditioning: "text"     # Cross-attention conditioning via CLIP
context: null            # No concatenation context
num_steps: 25
guidance_scale: 7.5
